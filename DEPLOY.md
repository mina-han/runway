# 배포 가이드

Vercel + Firebase 로 런웨이 대시보드를 올리는 순서입니다. 위에서부터 그대로 따라가면 됩니다.

## 무엇이 어디에 올라가는가

| | 로컬 `dashboard.html` | 배포판 `public/index.html` |
|---|---|---|
| 거래내역 168건 | 파일에 포함 | **없음** |
| 계좌번호 · 실명 | 파일에 포함 | **없음** |
| 5~7월 실적 분석 | 포함 | 없음 |
| 반복 스케줄 · 잔액 | 파일에 포함 | Firestore 에 **암호문**으로 저장 |
| 접근 | 없음 | 비밀번호 한 개 (계정 없음) |

> **왜 나눴나**
> 정적 파일에 비밀번호 창만 씌우는 건 보호가 아닙니다. 데이터가 파일 안에 있으면
> 비밀번호 없이 파일만 받아도 다 읽힙니다. 그래서 배포판에는 데이터를 아예 넣지 않고,
> Firestore 에서 받아오게 했습니다. `build.py` 는 빌드할 때마다 배포판에 데이터가
> 섞이지 않았는지 검사하고, 섞였으면 빌드를 중단합니다.
>
> **비밀번호도 JS 에서 단순 비교하면 의미가 없습니다.** Firebase 설정값은 공개라
> 규칙을 열면 누구나 REST 로 데이터를 긁어갑니다. 그래서 데이터 자체를 비밀번호로
> 암호화했습니다 — 서버에는 암호문만 있고, 비밀번호는 브라우저를 벗어나지 않습니다.
> (PBKDF2-HMAC-SHA256 310,000회 → AES-256-GCM)

> ### ⚠ 비밀번호 `0000` 은 사실상 공개입니다
> 경우의 수가 1만 개뿐이라, 암호문을 가져간 사람은 몇 초 만에 풉니다.
> 암호화는 **비밀번호만큼만** 강합니다.
>
> `.env` 의 `RUNWAY_PASSWORD` 를 긴 문구로 바꾸고 씨딩만 다시 하면
> **코드 수정 없이** 실제로 안전해집니다. 화면과 사용법은 그대로입니다.
> ```
> RUNWAY_PASSWORD=우리집런웨이2026보자
> python build/seed_firestore.py
> ```

---

## 1. Firebase 웹 앱 등록

Firebase 콘솔 → **프로젝트 설정(⚙)** → **일반** → 내 앱 → **웹(`</>`)** 추가.
호스팅 설정은 체크하지 않아도 됩니다. 나온 `firebaseConfig` 값을 복사합니다.

```bash
cp public/firebase-config.example.js public/firebase-config.js
```

`public/firebase-config.js` 를 열어 복사한 값을 채우고, `DATABASE_ID` 를
데이터베이스 이름(`runway`, 기본이면 `(default)`)에 맞춥니다.

> 이 파일의 `apiKey` 는 비밀이 아닙니다. Firebase 웹 키는 공개되도록 설계됐고
> 실제 보호는 로그인 + 보안 규칙이 합니다. 그래서 **커밋해야 합니다** (배포에 필요).
>
> ⚠ 다운로드하신 `firebase-adminsdk-*.json` 은 여기에 절대 넣지 마세요. 그건 서버 전용
> 관리자 키로 보안 규칙을 전부 우회합니다. `.gitignore` 로 커밋도 막아 뒀습니다.

## 2. 익명 로그인 켜기

콘솔 → **Authentication** → 시작하기 → **익명(Anonymous)** 사용 설정.

사용자가 볼 일은 없습니다. 페이지가 뒤에서 알아서 처리하고, 화면에는 비밀번호 칸만
나옵니다. 무작위 REST 조회를 막는 얇은 울타리 역할입니다 — 진짜 방어선은 암호화입니다.

## 3. 보안 규칙 게시

콘솔 → **Firestore Database** → **규칙** 에 `firestore.rules` 내용을 붙여넣고 **게시**.

문서에는 암호문만 있고, 쓰기도 '암호문 모양'을 갖춘 것만 통과합니다.
평문 필드를 끼워 넣거나 문서를 지우는 건 막혀 있습니다.

## 4. 비밀번호 정하고 데이터 올리기

```bash
pip install firebase-admin cryptography
```

`.env` 에서 비밀번호를 확인하거나 바꿉니다. 이 값으로 데이터가 암호화됩니다.
`.env` 는 커밋되지 않으니 실제 값은 그 파일에만 두세요.

```
RUNWAY_PASSWORD=<여기에 비밀번호>
```

씨딩 (`.env` 를 자동으로 읽으므로 환경변수를 따로 지정하지 않아도 됩니다):

```bash
python build/seed_firestore.py
```

관리자 키는 이 PC를 벗어나지 않습니다. 올라가는 건 잔액과 반복 스케줄뿐이고,
그마저도 암호문입니다. 거래내역 원본은 올라가지 않습니다.

> 비밀번호를 바꾸려면 `.env` 의 `RUNWAY_PASSWORD` 를 고치고 이 스크립트를 다시 실행하면
> 됩니다. 코드는 건드릴 필요 없습니다.

## 5. Vercel 연결

Node 가 설치돼 있지 않으므로 CLI 대신 **GitHub 연동**을 씁니다 (더 편하기도 합니다).

1. 먼저 커밋 & 푸시

   ```bash
   git add -A && git commit -m "Vercel 배포 설정" && git push
   ```

2. [vercel.com](https://vercel.com) 에 GitHub 계정으로 로그인
3. **Add New… → Project** → `mina-han/runway` **Import**
4. 설정은 건드리지 마세요. `vercel.json` 이 `public/` 을 배포하도록 이미 지정합니다
5. **Deploy**

이후 `main` 에 푸시할 때마다 자동 배포됩니다.

## 6. 확인

배포 URL 을 열어 비밀번호를 넣습니다. 소진일과 잔고 흐름이 보이면 성공입니다.
잔액을 고치면 우상단에 "저장됨"이 뜨고, 다른 기기에서 열어도 같은 값이 보입니다.

> **로컬에서 미리 보려면 반드시 `localhost` 로 여세요.** 암복호에 쓰는 WebCrypto 는
> 보안 컨텍스트(HTTPS 또는 localhost)에서만 동작합니다. `file://` 로 열면
> 비밀번호를 넣어도 열리지 않습니다.
> ```bash
> python -m http.server 8765 --directory public
> ```
> 그리고 http://localhost:8765 로 접속합니다. Vercel 은 HTTPS 라 그대로 동작합니다.

---

## 갱신

**새 명세서가 생겼을 때** — 스케줄까지 다시 계산합니다.

```bash
python build/analyze.py && python build/build.py && python build/seed_firestore.py
git add -A && git commit -m "8월 거래내역 반영" && git push
```

**잔액만 바뀌었을 때** — 배포된 사이트에서 직접 고치면 됩니다. 아무것도 실행할 필요 없습니다.

---

## 남는 위험

- **비밀번호가 유일한 방어선입니다.** `0000` 이면 암호문을 가져간 사람이 몇 초 만에
  풉니다. 긴 문구로 바꾸는 게 이 프로젝트에서 보안을 올리는 가장 효과적인 한 가지입니다.
- **URL 은 누구나 접근 가능합니다.** 비밀번호 화면까지는 열립니다.
  데이터는 암호문이라 비밀번호 없이는 읽히지 않습니다.
- **`public/` 에 다른 파일을 넣지 마세요.** 그 폴더는 전부 공개됩니다.
  `data.json` 이나 `dashboard.html` 을 실수로 넣으면 데이터가 그대로 노출됩니다.
- 익명 로그인이라 누구나 토큰을 얻을 수 있어, 악의적인 사람이 문서를 덮어써
  망가뜨릴 수는 있습니다(읽지는 못합니다). 그럴 땐 씨딩을 다시 하면 복구됩니다.
- 검색엔진 색인은 `robots.txt` 와 `X-Robots-Tag` 헤더로 막아 뒀습니다.
